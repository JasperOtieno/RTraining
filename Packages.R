#Packages for Rtraining


library(tidyverse) #for data cleaning, manipulation, transformation, visualization

library(haven) #for improting various types of data formats
library(magrittr) #for piping i.e %>% 

install.packages("gmodels") #
library(gmodels)

install.packages("table1")
library(table1)

library(readxl) #for importing xls dataset

